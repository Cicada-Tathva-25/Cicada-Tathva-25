<!DOCTYPE html>
<html>
<head>
    <title>CICADA 2025</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Exo:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
    <style>
        body{
            font-family: "Exo";
        }
        .navbar {
            background-color: black;
            color: #00cdaf;
            font-size: 20px;
            box-shadow: #00cdaf 0 0 10px;
        }
        #navbarNav ul {
            display: flex;
            justify-content: start;
            width: 100%;
        }
        .logoutli{
            margin-left: auto;
        }
        .logoutBtn {
                border: 2px solid black;
                border-radius: 5px;
                font-size: 20px;
                color: black;
                background-color: #00cdaf;
                font-family: "Exo";
                font-weight: bold;
                padding: 5px;
                padding-left: 20px;
                padding-right: 20px;
                /* margin-left: 900px; */
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg" id="navbarid">
        <a class="navbar-brand" href="#" style="color: #00cdaf;"><%= user.name %></a>
        <a class="navbar-brand" href="#" style="color: #00cdaf;"><%= user.email %></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/" style="color: #00cdaf;">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/profile" style="color: #00cdaf;">Profile</a>
                </li>
                <li class="nav-item logoutli">
                    <form action="/logout?_method=DELETE" method="POST">
                        <button class="logoutBtn" type="submit">Logout</button>
                    </form>
                </li>
            </ul>
        </div>
    </nav>
    <div class="content">
        <%- body %>
    </div>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>